/**
 * 브랜드 표기 단일 출처 (Single Source of Truth).
 *
 * 결정 (팀장, 2026-08-07): 서비스명 표기는 **'Hello Kiki'**(영문, 두 단어 띄어쓰기)
 *   단일로 통일한다. 한글 병기는 하지 않는다. 구표기 'Kikibooks' · '키키북스' ·
 *   'HelloKiki'(붙임)는 **전부 폐기**한다.
 *
 * 배경: 2026-06-12 플랫폼 명칭 변경 결정(키키북스 → 헬로키키) 이후 문서(PLAN v2.0·
 *   PRD v2·license-rules v1.1)만 신 브랜드로 바뀌고 UI·메타데이터는 구 브랜드로
 *   남아 있었다(docs/backlog.md §7.4 (a) "전수 반영 잔여"). 유료 베타(8/31) 전에
 *   한 번에 정리하는 편이 총비용이 낮다는 팀장 판단으로 전수 치환했다.
 *
 * ★ `import 'server-only'`를 붙이지 않는다 — 랜딩 헤더/푸터 등 클라이언트 컴포넌트도
 *   이 상수를 참조해야 하므로 서버 전용으로 잠그면 안 된다.
 *
 * 사용 규칙: 화면에 브랜드명을 렌더하거나 metadata title/siteName에 넣을 때는
 *   문자열을 새로 쓰지 말고 **반드시 본 상수를 참조**한다. 다음 리네이밍 때
 *   이 파일 1곳만 고치면 되도록 유지한다.
 *
 * 예외(무접촉): `docs/`·ADR·PLAN.md의 구표기는 **이력 문서**이므로 소급 수정하지 않는다.
 */

/** 서비스명. 화면·metadata·OG 이미지 전부 이 값을 쓴다. */
export const BRAND_NAME = 'Hello Kiki';

/** 서비스 한 줄 소개. 랜딩 title·OG alt에서 BRAND_NAME과 함께 쓰인다. */
export const BRAND_TAGLINE = '우리 아이의 첫 영어 그림책 서재';
