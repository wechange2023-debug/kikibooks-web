# OCR 금지 근거 규명 및 정확도 실측 (2026-07-10)

> 작업지시서 2026-07-10 (1). 읽기 전용 조사 + 로컬 OCR 실측(임시 경로, 산출물 삭제 완료).
> DB·Storage·코드(.ts/.tsx/.py) 변경 0건. AWS 호출 0건.

## STEP 1 — "OCR 금지"의 출처 규명 (판정 MENTION)

레포 전체(docs/·scripts/·*.md·ADR 전량)를 `OCR|ocr`, `인페인팅|inpaint(ing)`,
`텍스트 제거|text removal|baked`로 전수 grep한 결과:

### OCR 히트 (레포 정본 2건 + 무관 2건)

1. `docs/adr/0023-ai-features-and-tts-policy.md:134`
   > "**대상 코호트**: Book Dash **v1 html 39권**(텍스트 추출 가능 — §1.1). v2 asb_native 206권은 **OCR 별도 2차 트랙**(본 Amendment 범위 외)."
   - (a) TTS 대상 코호트를 39권으로 한정하는 **결정**이며, OCR에 대해서는 "별도 2차 트랙"
     이라는 **이연 언급**. 금지가 아니라 **범위 외 유보**.
   - (b) 사유 명시 없음("본 Amendment 범위 외"가 전부).
   - (c) 라이선스/정확도 사유 모두 **미명시**.
2. `scripts/tts_pilot/HANDOFF.md:11` (미커밋 인계 메모) — 위와 동일 문장. **언급**.
3. `scratchpad/bloom_staging_insert.sql:773`, `scratchpad/bloom_1060_dryrun.json:7393`
   — Bloom 도서 ID 문자열 `aV0BfOCRdO`에 "OCR"이 부분 문자열로 포함된 **무관 히트**.

### 인페인팅 / 텍스트 제거

- `인페인팅|inpaint|inpainting`: **0건**. `텍스트 제거|text removal`: **0건**.

### baked 히트 (7파일 — 모두 "WP판은 baked-in이라 A안 부적합" 기록, OCR 결정 아님)

- `docs/adr/0035-bookdash-self-viewer.md:118`
  > "예외: WP(bookdash.org) 재확보분은 본문 텍스트가 이미지에 인쇄(baked-in)되어 **A안 부적합**."
  → A안 코호트 편입 불가라는 **결정**. 사유 = A2(무텍스트 전제). OCR 언급 없음.
- `docs/adr/0036-bookdash-image-storage.md:173-176`
  > "WP baked-in 원본은 생존하므로 '영구 소실'은 아님. 편입하려면 별도 결정(baked-in 수용 또는 다른 무텍스트 소스 발굴)이 필요하다 — **본 Amendment는 지위 기록만 하고 결정하지 않는다**."
  → 명시적 **미결** 기록.
- `docs/recon/2026-07-09-wp-page-mapping-gate.md:132,148`,
  `docs/recon/2026-07-09-viewer-architecture-evidence.md:33,144,193,217` — 실측 **언급**.

### 판정

**STEP1 = MENTION** — OCR은 ADR-0023 Amd#1 :134에 "별도 2차 트랙"으로 **언급**될 뿐,
"OCR을 하지 않는다/금지한다"는 **결정은 레포 어디에도 기록되어 있지 않다**. 인페인팅은
언급조차 0건. "OCR·인페인팅 안 함(H5)"의 유일한 출전은 작업지시서 (7)의 팀장 결정 문안
(대화 내 지시)이며, 레포 정본에는 근거 문서가 없다.

## STEP 2 — 라이선스 검토

- Book Dash 적재 라이선스 = **cc-by-4-0**: `scripts/sync_book_dash_v2.py:67`
  `LICENSE_CODE = "cc-by-4-0"`(:699에서 그대로 적재). DB 직접 조회는 미실행(3자 구조).
- `docs/guidelines/license-rules.md:25`
  > "| `cc-by-4-0` | ✅ 가능 | ✅ 필수 | ✅ **가능** | Book Dash, GDL 주력 |"
  (열 순서: 영리 사용 / 어트리뷰션 의무 / **변경·2차 저작물** / 비고)
- 같은 문서 §4.4(:137): TTS 낭독은 "원본 텍스트의 2차 저작물(derivative)"로서
  "원본 라이선스 의무를 그대로 승계" — 즉 2차 저작물 제작 자체는 허용 전제.
- **라이선스 전문: 로컬에 전문 없음**(레포 내 CC BY 4.0 legalcode 사본 부재. 웹 검색 미수행).
  이미지에서 텍스트를 추출해 별도 표시하는 행위를 제한하는 조항의 유무는 전문 기준으로 **미확인**.
- 코드 제약: `scripts/lib/attribution.py` = 어트리뷰션 텍스트 빌더 + 라이선스 4종
  화이트리스트뿐, 추출 관련 제약 없음. `scripts/verify_licenses.py` = NC/ND 드리프트
  감지 전용, 추출 관련 제약 없음.

## STEP 3 — a-tiny-seed OCR 정확도 실측 (판정 O-HIGH)

- 정답: `scripts/tts_pilot/out/a-tiny-seed.json` 12면. 입력: **WP판**(baked-in) 본문 12장
  — `a-tiny-seed_en_20200616_page4..15.jpg`(소문자 `_page{n}` 규칙: n+1=PDF, 서비스=PDF−4;
  검증된 매핑 규칙 그대로 적용). 임시 경로에만 다운로드.
- 엔진: **tesseract 5.4.0**(UB-Mannheim, winget 로컬 설치, eng) — 무료·로컬. easyocr 미사용.

| 페이지 | 정답 단어 | 단어 오류 | 단어 정확도 % | 문자 정확도 % | 완전 일치 |
|---|---|---|---|---|---|
| p01 | 26 | 0 | 100.00 | 100.00 | O |
| p02 | 25 | 1 | 96.00 | 98.57 | X |
| p03 | 40 | 0 | 100.00 | 100.00 | O |
| p04 | 44 | 0 | 100.00 | 100.00 | O |
| p05 | 43 | 0 | 100.00 | 100.00 | O |
| p06 | 38 | 0 | 100.00 | 100.00 | O |
| p07 | 34 | 0 | 100.00 | 100.00 | O |
| p08 | 38 | 5 | 86.84 | 91.20 | X |
| p09 | 43 | 0 | 100.00 | 100.00 | O |
| p10 | 29 | 1 | 96.55 | 99.46 | X |
| p11 | 35 | 0 | 100.00 | 100.00 | O |
| p12 | 17 | 2 | 88.24 | 93.83 | X |

- **전체: 정답 412단어 중 오류 9 → 단어 정확도 97.82%**. 완전 일치 **8/12** 페이지.
- 오류 9건의 성격: **치환 1건 + 삽입 8건**. 삽입 8건은 전부 일러스트 영역을 글자로
  오인한 노이즈(본문 문장 훼손 아님).
- 대표 오독 3건:
  1. p10: `Wangari's` → `Wangariss` (아포스트로피 오독 — 유일한 실질 치환)
  2. p08: 일러스트 노이즈 삽입 5건 `es`, `ow`, `\iQ`, `Fer'`, `VAI`
  3. p02: 노이즈 삽입 1건 `®`
- **bounding box: 예** — TSV 출력에서 word 레벨(level=5) 좌표+신뢰도 산출. 샘플(p01):
  `In (left=2168, top=469, w=66, h=55, conf=96.1)` / `a (2257,487,31,37, 96.1)` /
  `village (2307,466,209,76, 95.5)`
- 정리(3-5): 임시 다운로드 12장 + OCR 산출물(txt/tsv) 총 36파일 삭제 완료
  (`Test-Path` = False 확인).

**STEP3 = O-HIGH (단어 정확도 97.82%)** — 페이지별 편차: 8/12 페이지 100%,
최저 p08 86.84%(노이즈 삽입 기인).

## STEP 4 — 사람 작성 방식의 작업량 산정 (계산만)

- 대상 구분:
  - **작성 대상 = WP-only 154권** (신규 157 − drift 3; drift 3권은 구 slug의 v1 JSON 보유).
    ★ 지시서의 "157권"은 drift 3권의 JSON 보유 반영 전 수치 — **154권으로 정정**.
  - **Wordless 2권(springloaded·i-can-dress-myself, WP term 643)**: 원래 글 없는 책 —
    작성 대상 아님 **확인**(영어 목록 206 밖. springloaded는 alt 텍스트 JSON 12면 별도 보유).
  - v1 무텍스트책 4권(lion·hugs·katiitis·it-wasnt-me): GH HTML 200이나 본문 텍스트 0 —
    원래 글 없는 책인지 **미확인**(WP판 육안 필요, 확인 시 작성 대상 아님).
- 실측 산정 (탐침 로그 wpP + 기존 JSON):
  - 대상 **154권**, 총 **2,316페이지** — 153권 13~35장(평균 15.1) + the-baby-book 1장
    (불완전 카탈로그 의심). 책당 평균 **15.0면**.
  - 기존 48권(206 내 JSON): 총 81,978자 / 비-empty 575면(전체 578면) →
    **페이지당 평균 142.6자**, 책당 평균 1,707.9자.
  - 외삽: 154권 ≈ **33.0만 자** (2,316면 × 142.6자; empty 면 비율 0.99 반영 시 32.8만 자).
- 그릇 스키마(`scripts/tts_pilot/out/{slug}.json`) 실측:
  `[{"page": int(1..N), "image_url": str, "text": str}]` — 다면 텍스트는 `\n` 구분,
  아포스트로피는 curly(`'`→`’`) 사용, empty 면은 목록에 포함하되 `text: ""`.
- 사람 입력에 추가로 필요한 항목(사실 나열, 도구 설계 아님):
  - 서비스 page 번호 ↔ 입력 이미지(WP full-size URL) 매핑 기준
  - empty 면 판정 기준(그림만 있는 면 = `text: ""`)
  - 표기 규약(따옴표 종류·줄바꿈 `\n`·문장부호) — TTS·marks 오프셋이 이 문자열 기준
  - 작성 범위 경계(표지·속표지·판권면 제외 규칙)
  - 검수 상태 필드 — 현 스키마에 없음

**STEP4 = 작성 대상 154권 / 총 2,316페이지** (외삽 33.0만 자 ≈ 기존 49권 총량 82,795자의 약 4배)

## 오케스트레이터 판단 요청 사항

1. **H5 재검토 여부**: "OCR 금지"는 레포 정본에 결정 기록이 없고(판정 MENTION),
   대조군 실측은 O-HIGH(97.82%)다. 오류 9건 중 8건이 일러스트 노이즈 삽입이라
   신뢰도(conf) 임계 필터로 추가 억제 여지가 있다. OCR 초벌 + 사람 검수 vs 순수 사람
   작성(33.0만 자)의 선택은 팀장 결정 사안.
2. 라이선스 측면: 로컬 문서 기준 cc-by-4-0은 변경·2차 저작물 허용으로 기록되어 있으나
   **전문 사본이 로컬에 없다** — 전문 확인 1회(팀장 또는 승인된 웹 확인)를 권장.
3. 대조군 1권(활자체 12면)의 실측이므로, 도입 결정 전 **서체가 다른 2~3권 추가 실측**으로
   일반화 가능성을 확인할지 여부.
4. the-baby-book(WP 1장)·the-three-doof-doofs(35장)·my-special-hair(34장)는 편입 전
   개별 gate 확인 대상.

*문서 끝.*
